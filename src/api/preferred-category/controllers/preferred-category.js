'use strict';

/**
 * preferred-category controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::preferred-category.preferred-category');