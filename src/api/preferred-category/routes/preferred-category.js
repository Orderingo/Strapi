'use strict';

/**
 * preferred-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::preferred-category.preferred-category');