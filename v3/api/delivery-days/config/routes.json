{
  "routes": [
    {
      "method": "GET",
      "path": "/delivery-days",
      "handler": "delivery-days.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/delivery-days/count",
      "handler": "delivery-days.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/delivery-days/:id",
      "handler": "delivery-days.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/delivery-days",
      "handler": "delivery-days.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/delivery-days/:id",
      "handler": "delivery-days.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/delivery-days/:id",
      "handler": "delivery-days.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
